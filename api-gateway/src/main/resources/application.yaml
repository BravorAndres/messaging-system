spring:
  application:
    name: api-gateway
  datasource:
    url: jdbc:mysql://localhost:3306/messaging_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

server:
  port: 8080


api:
  security:
    key: my-super-secret-key-123